import React, {
  useState,
  useContext,
  useLayoutEffect,
  useEffect,
  useCallback,
} from 'react';
import {
  SafeAreaView,
  Text,
  View,
  TouchableOpacity,
  Image,
  Dimensions,
  TextInput,
  Alert,
  StyleSheet,
  ScrollView,
} from 'react-native';

const CustomerService = () => {
  return <Text>개발자에게 문의하기</Text>;
};

export default CustomerService;
