import React, {
  useState,
  useContext,
  useLayoutEffect,
  useEffect,
  useCallback,
} from 'react';
import {
  SafeAreaView,
  Text,
  View,
  TouchableOpacity,
  Image,
  Dimensions,
  TextInput,
  Alert,
  StyleSheet,
  ScrollView,
} from 'react-native';

const Notice = () => {
  return <Text>공지사항</Text>;
};

export default Notice;
